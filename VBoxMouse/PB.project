INSTALLDIR = "";
COMPILEROPTIONS = "";
GENERATEMAIN = YES;
DOCICONFILES = ();
FILESTABLE = {
    OTHER_SOURCES = (Makefile, Makefile.postamble, Makefile.preamble);
    OTHER_RESOURCES = (Default.table, Info.rtf);
    OTHER_LIBS = (Indexing_s);
    STRINGS_FILES = (Localizable.strings);
    TOOLS = (VBoxMouse_reloc.tproj);
    SUBPROJECTS = ();
};
BUNDLE_EXTENSION = config;
LINKEROPTIONS = "";
DOCEXTENSIONS = ();
PROJECTTYPE = Bundle;
PROJECTVERSION = 0.9;
LOCALIZABLE_FILES = {
    Info.rtf;
    Localizable.strings;
};
PROJECTNAME = VBoxMouse;
LANGUAGE = English;
